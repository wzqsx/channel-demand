# 渠道要货 · 使用说明

本地前端工具（Vue 3 + Pinia），数据保存在浏览器 `localStorage`，无后端。

## 安装与启动（另一台 Mac）

完整步骤（装 Homebrew / Node / Git → clone → `npm install` → `npm run dev` → 打开网页）见仓库根目录 **[README.md](./README.md)**，请按顺序做，避免只执行 `npm install` 却没装 Node。

日常再次启动：

```bash
cd ~/channel-demand   # 按你实际目录
npm run dev
```

浏览器打开终端提示的地址，一般是：http://localhost:5173/

**重要**：请固定用 `localhost`，不要混用 `127.0.0.1`。二者在浏览器里是两套独立数据，换地址会像「数据被清空」。更新代码前建议点右上角「导出备份」。

本机已上传新代码后，另一台电脑同步：

```bash
cd ~/channel-demand
git pull
npm install   # 依赖没变可跳过
npm run dev
```

## 建议使用顺序

1. **公司主体**：维护经营主体（如新增「110」）
2. **仓库**：仓库必须挂到所属主体
3. **渠道**：渠道挂同主体仓库，并设置优先级
4. **商品**：维护编码、预警阈值；箱规勾选组合并填基础瓶规 + 换算比例
5. **库存导入**：按周全量替换库存（含在途；瓶规/箱规都可导）
6. **渠道要货**：按周导入要货（按瓶规），验库存后审批
7. **销货核对 / 渠道要货总览**：看完成率与排名
8. **缺货与预警**：按主体查看缺货与库存预警（数量展示如 `10箱零3瓶`）

## 关键规则

- 库存校验、缺货缺口只在**同一主体**内计算，不跨主体混仓
- 渠道优先级只在**同一主体**内比较
- 完成率 = 销货 ÷ 要货（百分比）
- 要货周期：自选**起始日**，结束日自动为起始日后第 6 天（共 7 天，如 7月30日 — 8月5日）；默认可从今天起算，也可选未来周期（提交时确认）
- **瓶规 / 箱规**：要货按瓶规；箱规库存按比例折进瓶规后再验库存、算缺货
- 缺货预警数量展示按瓶规「每箱瓶数」换算（如 `10箱零3瓶`）
- 库存仅作周初参考记账，不滚动扣减

## 自检

```bash
npm run selfcheck
npm run build
```
